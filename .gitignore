# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Excel-Sperrdateien (entstehen beim Öffnen einer .xlsx)
~$*

# Lokale Arbeitsdateien, die nicht ins Repo gehören:
# Preis-/Mapping-Tabellen von Guido und Leadprint-Exporte (Binärdaten, teils groß).
# Der verbindliche Stand liegt in src/data/pricingConfig.default.json bzw. im
# geteilten Preisstand — die Tabellen sind nur Zulieferung.
*.xlsx
*.xls
Leadprint_Export/

# Design-Handoff-Bundle (Prototyp-HTML aus dem Design-Tool, kein Projektcode).
# Der umgesetzte Stand ist der Code; die Referenz bleibt lokal.
design_handoff_gc_tools_ui/
.claude/
